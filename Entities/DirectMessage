{
  "name": "DirectMessage",
  "type": "object",
  "properties": {
    "match_id": {
      "type": "string",
      "description": "Reference to match request"
    },
    "sender_id": {
      "type": "string"
    },
    "sender_name": {
      "type": "string"
    },
    "receiver_id": {
      "type": "string"
    },
    "content": {
      "type": "string"
    },
    "is_read": {
      "type": "boolean",
      "default": false
    }
  },
  "required": [
    "match_id",
    "sender_id",
    "receiver_id",
    "content"
  ],
  "rls": {
    "create": {
      "sender_id": "{{user.id}}"
    },
    "read": {
      "$or": [
        {
          "sender_id": "{{user.id}}"
        },
        {
          "receiver_id": "{{user.id}}"
        }
      ]
    },
    "update": {
      "$or": [
        {
          "sender_id": "{{user.id}}"
        },
        {
          "receiver_id": "{{user.id}}"
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "sender_id": "{{user.id}}"
        },
        {
          "receiver_id": "{{user.id}}"
        }
      ]
    }
  }
}