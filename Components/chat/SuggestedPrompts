import React from "react";
import { Button } from "@/components/ui/button";
import { Heart, Briefcase, GraduationCap, Users, Sparkles, Home } from "lucide-react";

const prompts = [
  { 
    icon: Heart, 
    text: "I need help with emotional overwhelm",
    color: "from-pink-400 to-rose-400"
  },
  { 
    icon: Briefcase, 
    text: "Work-life balance is difficult right now",
    color: "from-blue-400 to-cyan-400"
  },
  { 
    icon: GraduationCap, 
    text: "Struggling with academic stress",
    color: "from-purple-400 to-indigo-400"
  },
  { 
    icon: Users, 
    text: "Relationship concerns I want to talk about",
    color: "from-teal-400 to-green-400"
  },
  { 
    icon: Sparkles, 
    text: "Need self-care and mindfulness tips",
    color: "from-yellow-400 to-orange-400"
  },
  { 
    icon: Home, 
    text: "Feeling lonely and disconnected",
    color: "from-indigo-400 to-purple-400"
  },
];

export default function SuggestedPrompts({ onSelect, onClose }) {
  return (
    <div className="absolute bottom-20 left-0 right-0 glass-card border border-white/30 rounded-t-3xl p-6 max-h-[60vh] overflow-y-auto">
      <div className="max-w-lg mx-auto">
        <div className="flex items-center justify-between mb-4">
          <h3 className="font-semibold text-gray-900">How can Luna help you today?</h3>
          <button 
            onClick={onClose}
            className="text-gray-500 hover:text-gray-700"
          >
            ✕
          </button>
        </div>

        <div className="grid gap-3">
          {prompts.map((prompt, index) => {
            const Icon = prompt.icon;
            return (
              <button
                key={index}
                onClick={() => {
                  onSelect(prompt.text);
                  onClose();
                }}
                className="flex items-center gap-3 p-4 bg-white/20 rounded-2xl hover:bg-white/30 transition-all duration-300 hover:scale-[1.02] text-left"
              >
                <div className={`w-10 h-10 rounded-xl bg-gradient-to-br ${prompt.color} flex items-center justify-center flex-shrink-0`}>
                  <Icon className="w-5 h-5 text-white" />
                </div>
                <span className="text-sm font-medium text-gray-900">
                  {prompt.text}
                </span>
              </button>
            );
          })}
        </div>
      </div>
    </div>
  );
}